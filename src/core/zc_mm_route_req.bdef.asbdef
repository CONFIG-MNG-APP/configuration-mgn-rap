projection;
strict ( 2 );
use draft;

define behavior for ZC_MM_ROUTE_REQ alias RouteReq
{
  use create;
  use update;
  use delete;

  use action Approve;
  use action Reject;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}