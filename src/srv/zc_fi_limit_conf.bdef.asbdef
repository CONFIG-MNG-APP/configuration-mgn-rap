projection;
strict;
use draft;

define behavior for ZC_FI_LIMIT_CONF
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}