projection;
strict ( 2 );
use draft;

define behavior for ZC_MM_ROUTE_CONF alias RouteConf
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}